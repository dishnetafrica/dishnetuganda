# Copy to .env (gitignored) before first `docker compose up -d`.
# Fixes the Stalwart admin credentials instead of a random first-boot password.
# Generate:  echo "admin:$(openssl rand -base64 18)"
STALWART_ADMIN=admin:CHANGE_ME
