# 69 — Decision index

**Navigational only.** No analysis, no recommendations, no reconciliations, no
decisions. Every statement here points at a document that holds the
authoritative version. Where this index and a decision document disagree, the
decision document is right.

Covers the F6 voucher work, docs/63–68, and the architecture freeze it sits
under.

---

## The decision set

| # | Decision | State | What was decided | Authoritative record | Evidence |
|---|---|---|---|---|---|
| — | **F1–F13 architecture freeze** | **FROZEN** | 13 items, unfrozen only by explicit amendment to docs/53 §5 | **docs/53** | — |
| **1** | **AAA publication lifecycle** | **CLOSED** | **Model B** — the AAA credential is published at redemption/activation, not at voucher issue | **docs/66 §1** | docs/65 §3 (both models measured across all three layers), §4 |
| **2a** | **Cross-customer AAA security floor — and the mechanism enforcing it** | **CLOSED** | **C-b — site-keyed dynamic SQL source-address restriction**, using `dnb_cred_site`, `dnb_site_nas` and the `EXISTS` predicate against the authorized NAS/source-address set. **Huntgroups retired as a production candidate** | **docs/70 §8** (requirement: docs/68 §2.4) | docs/70 §2, §8.1; docs/68 §2.4b–g |
| **2b** | **Product / site-binding policy** | **CLOSED** | **A / SITE-BOUND** — a voucher is bound to its issuing site and valid only against the NAS set authorized for that site. Does **not** imply one router per site | **docs/68 §2.5** | docs/68 §2.5a, §2.6, §2.6a–b |
| **3** | **Portal contract** | **CLOSED** | **P2** — generated AAA credentials; the commercial voucher code never enters the AAA credential path | **docs/67** | docs/65 §6, §12.4 |
| **3a** | **Voucher issuance requires a site** | **CLOSED** | Under 2b a voucher may **not** be issued without an issuing site — Option A, reject at creation. `mt_vouchers.site_id` becomes `NOT NULL` | **docs/77 §1** | docs/76 Part A |
| **3b** | **Batch site invariant** — may a voucher batch exist with no site? | **OPEN SCHEMA DECISION** | `mt_voucher_batches.site_id` is **NULLABLE** today. B1 leave nullable / B2 make NOT NULL. **Not a pending consequence of 3a** — it awaits production evidence and explicit approval, and is **not** carried in the migration plan | **docs/78 §4.2** | docs/77 §2 |
| **4** | **Front-desk activation** | **OPEN — business decision** | Whether an operator may activate on a guest's behalf | **docs/67 §7** | docs/64 §2 Q5 |
| **5** | **Rate limits and timing** | **OPEN** | Numbers only; the shape is settled | **docs/65 §9**, docs/67 §10 | docs/65 §9.2, §12.4 |
| **6** | **Retention** | **OPEN** | Two separate questions: the control plane's attempt store, and `radpostauth` | **docs/66 §7** | docs/65 §12.4 |
| **7** | **AAA publication authority and reconciliation** | **CLOSED** | Dedicated **AAA Publisher**, with the documented privilege boundary and reconciliation principles | **docs/66 §2** | docs/65 §12, docs/68 §2.4c |

---

## 2a — the distinctions that must not collapse

**The mechanism is now chosen.** docs/70 §8 holds the decision; docs/68 §2.4
holds the requirement it enforces. Three distinctions still matter.

| | |
|---|---|
| **Security requirement** | **PROVEN.** A credential must not authenticate outside its authorized NAS set, and FreeRADIUS can enforce that from a server-derived source address |
| **Production mechanism** | **CHOSEN: C-b**, site-keyed (`dnb_cred_site` + `dnb_site_nas` + the `EXISTS` predicate). docs/70 §8 |
| **Chosen ≠ built** | **Nothing is implemented.** The two tables do not exist, the production authorize query is still username-only, and the query change is a separately authorized step. docs/70 §8.2–8.3 |
| **Measured ≠ measured in production** | Every measurement behind the decision ran on a **disposable** instance (docs/70 §2). **Production has never been touched** and the mechanism has never run there. docs/70 §5.2 |
| **Huntgroups** | **RETIRED as a production candidate.** They were measured viable (E7, E8) but cost a full FreeRADIUS restart per mapping change in **both** directions — E9 (granting) and **E10** (removing, so stale authorization persists until that restart). E10 is why they were retired. They remain recorded for evidence value only; **do not build on them** |
| **C-a, C-c, C-d** | never measured, and now **unnecessary** — a mechanism has been chosen |

Evidence items **E1–E9** are enumerated in docs/68 §2.4c; **E10** and **S1–S9**
are in docs/70 §2, summarised in docs/70 §8.1. All are citable by number.

## Open business-model observation — not reconciled

**The ISP/operator hierarchy.** It informed Decision 2b's rationale — a customer
may serve unrelated downstream businesses, so customer-level commercial
ownership does not become customer-wide voucher scope (`docs/68` §2.5). It
remains a business-model input and is **not** a tenant layer in the
architecture. C20 evidence records that a DishNet customer may itself be an ISP
or operator managing its own downstream customers:
**DishNet → ISP/operator customer → sites → downstream users**, with each site
generating its own voucher stock. This layer is **new relative to the frozen
documents** and has deliberately **not** been reconciled into them. It is an open
business-model observation, not an architecture change. (`docs/68` §2.6)

---

## Implementation dependencies that must not be forgotten

| | Dependency | Where |
|---|---|---|
| **D-1** | **The site→NAS mapping is a second state path.** Under the chosen mechanism it is `dnb_site_nas` in the `radius` database, written on the **device-provisioning** lifecycle — *not* the publication lifecycle. It carries **site isolation and cross-customer isolation at once**, so the AAA Publisher must **not** be able to write it | docs/70 §5.1, §8.4; docs/68 §2.4d; pointer at docs/66 §9a |
| **D-6** | **The Decision 7 extension — a narrowly scoped provisioning writer** for `dnb_site_nas`. Designed in docs/71; **awaiting approval and blocked**. **N10/P-1** is measured exploitable — and **from the request role, not just `dnb_admin`** (docs/73 §1.1) — with a recommended remediation in **docs/73 §4** awaiting approval; **Q2** (may one router serve two sites?) is **NOT ESTABLISHED** and blocks the `dnb_site_nas` key | **docs/79**, docs/78, docs/77, docs/76, docs/75, docs/74, docs/73, docs/72, docs/71 |
| **D-2** | **A NAS identifier must exist in the control plane** and map to a device. It does not today | docs/68 §2.7 |
| **D-3** | **Invariant C1** — generated AAA credentials, independent of the voucher in every respect | docs/68 Part 1 |
| **D-4** | The **publication state machine** — `activating`, `activation_failed`, `revoking` — must not be collapsed into a generic HTTP 500 | docs/66 §4, docs/67 §4.2 |
| **D-5** | **Publication must be idempotent**; the AAA schema will not enforce it | docs/66 §2.6, docs/65 §12.3 |

---

## Deliberately deferred

| | Item | State | Requirements |
|---|---|---|---|
| **1** | **`s1_s2_probe.php` repair** | deferred since `bf5310b` | A separate **test-maintenance change**. Four requirements recorded in **docs/65 §15** |
| **2** | **C1 guard tests** | not yet written | Required **at** the F6 implementation gate, not after it. Specified in **docs/68 Part 1**, and must now also assert that **no client-asserted value reaches the authorize query** (docs/70 §5.3, §8.2 item 4) |

---

## Document map

| Document | What it is |
|---|---|
| **docs/53** | the architecture freeze, F1–F13 |
| **docs/63** | F6 voucher-redemption audit — the original finding |
| **docs/64** | F6 actor model and disposable prototype |
| **docs/65** | lifecycle evidence and the open-decision set. Rows 1, 3 and 7 struck through with pointers; §12 holds the AAA-boundary evidence |
| **docs/66** | **Decision record: Decisions 1 and 7** |
| **docs/67** | **Decision record: Decision 3** (amended once by docs/68 §3) |
| **docs/68** | credential invariant C1; **Decision 2b (closed)**; Decision 2a's requirement and evidence (§2.4a–g) |
| **docs/69** | this index |
| **docs/70** | **Decision record: Decision 2a (closed)** — the sixteen-requirement comparison, E10 and S1–S9, the reasoning, and the accepted consequences that are **not yet authorized to build**. §2 carries a correction to S2's reading |
| **docs/71** | **Decision 7 extension — design and evidence gate (awaiting approval).** The provisioning writer's security boundary, threat cases T1–T10, the privilege model, invariants N1–N9, findings **P-1/P-2/P-3**, and questions Q1–Q6. Builds nothing |
| **docs/72** | **Evidence gate:** P-1 **measured** (`DOES NOT WORK / exploitable`), Q2 **NOT ESTABLISHED**, both cardinality branches, and invariant **N10**. Chooses no schema |
| **docs/73** | **N10 remediation design (awaiting approval):** ten measurements M1–M10, the three candidates across thirteen dimensions, the eight-state matrix, and the recommendation — composite FK `MATCH SIMPLE` + a one-table CHECK + a validating backfill. Q2 held open. Implements nothing |
| **docs/74** | **N10 backfill data state:** fixture census (0 violations, 0 partial nulls), suite impact (718 green **with** the constraints), the five backfill classes, and the scope finding that `mt_plans`, `mt_voucher_batches` and **`mt_vouchers`** carry the same untied pair. **Production state NOT ESTABLISHED** |
| **docs/75** | **Customer/site ownership scope audit:** which tables genuinely need the invariant. `mt_devices`, `mt_vouchers`, `mt_voucher_batches` **yes** (one chain); `mt_plans` **no** (commercial scoping, read by nothing). All three measured able to hold a cross-customer pair. Also: `mt_vouchers.site_id` is NULLABLE, which Decision 2b cannot describe. Changes nothing |
| **docs/76** | **The site-less voucher + the proposed invariant set.** Measured: *every* voucher the suite creates is site-less (30/30), and such a voucher is a sellable object that can never work, indistinguishable from an invalid code. Options A/B/C for the domain decision, the three-table invariant set with NULL semantics, and six migration prerequisites. Decides nothing |
| **docs/77** | **Decision record (voucher site-less issuance CLOSED) + the operator census protocol.** Corrects the scope note: `mt_voucher_batches.site_id` is **NULLABLE**, not NOT NULL. Carries `tools/audit/production_census.sql` — read-only, self-tested, with a blinding guard. No migration |
| **docs/78** | **Census attempted and NOT obtained** — no SSH client, no DSN, egress 403, and the Phase 0 database is loopback-only, so it is unreachable **in principle** from here. Carries the operator runbook, the conditional migration/backfill plan, and **Decision B framed but not taken** |
| **docs/79** | **Operator handoff** — the copy-paste procedure for running the census: command, connection without exposing credentials, role requirements, how to confirm the authoritative database, what to return, and the read-only warning |
| **docs/80** | **F6-A application build plan** — the survey, the two blocking decisions, architecture changes A1–A7, the adapter boundary (simulators may never masquerade as a router), build order P1–P7, and the hardware register H1–H7. **F6-B stays gated** behind `DN_ALLOW_REAL_BINDINGS=yes-f6b-authorized` |
| **docs/81** | **Decision records 8 and 9** — staff identity is a **separate** boundary from `mt_principals`, and **Reseller is not a tenant** or tenant-like security scope |
| **docs/82** | **Customer PWA wiring** — the screens bound to the real API, the three missing contracts G1–G3 named rather than invented, and the correction that the API returns **named keys**, not `{items:[]}` |
| **docs/83** | **Admin cross-customer read boundary — design** — the read surface R1–R12, the table/column map, six options weighed, and the finding that `mt_devices_samplable()` already implements the recommended pattern. Decisions **D-1 accepted, D-2 taken** (no `payload`, `last_error` or `detail` through the general projection) |
| **docs/84** | **Admin write-boundary audit** — twenty-four state-changing operations across fourteen dimensions, findings **F-1…F-6**, the audit and intent requirements, the hardware-dependent operations, and the proposed boundary with its binding order. Open decisions **W-1…W-6**. Changed no code |
| **docs/85** | **Admin write remediation — W-1/W-2/W-3 implemented** (migration 020, development/test schema only). Audit written **inside** each of the seven functions by a narrow `dnb_def_audit` writer no HTTP role may call; the customer/site invariant as a **constraint** so it binds the direct-UPDATE path; and `dnb_adminwrite` with zero table DML. 109 new assertions. **No Admin write route is bound.** New findings **F-7** (`mt_voucher_redeem` unaudited) and **F-8** (six caller-written audit sites). **No backfill; production untouched** |
| **docs/86** | **F-7 / F-8 evidence audit** — the redemption path traced end to end and the six customer-API audit sites inspected. Measured: **redemption is wired to nothing** (`VoucherService::redeem()` has no caller), the AAA registry row is written **at issue**, and `radius_username` is a **reversible transform of the voucher code** — contradicting Decisions 1 and 3. The customer audit sites *are* transactional; the defect is **skippability**, measured. Four mutating routes audit nothing. Actor taxonomy is already fixed by the table's own CHECK (`principal | staff | system`). Questions **Q-F7-1…7** and findings **F-8.1…5**. Changed nothing |
| **docs/87** | **F-7 remediation design (design only)** — the current non-conforming flow, the target Model B + P2 + Decision 7 flow, the full gap list across control plane / `radius` database / application, the actor model (**the existing `principal\|staff\|system` taxonomy suffices; `guest` rejected**), the F-7 audit design, a **separate** F-8 stream, and a nineteen-item execution test plan. Records the rule that **`dnb_cred_site` is written from `voucher.site_id`, never from the guest-asserted NAS**. Seven decisions **P-1…P-7** needed before implementation. Builds nothing |
| **docs/88** | **Decision 5 packet + Decision 4 evidence.** Records **D-1a and P-1 as CLOSED**. Finds that **Decision 5 has a refuted premise**: `docs/65` §9.3 made per-NAS primary, `docs/68` §2.2 refuted the claim it rested on, `docs/65` §9.2 says per-code does not bound enumeration and §9.3 says source IP is not primary — so no dimension is both trustworthy and effective. Four candidate resolutions R-a…R-d; **R-b depends on unverified MikroTik hardware**. Eight-parameter table with documented constraint, proposal, and both reasons; row 1 labelled **an engineering guess** because publication latency has never been measured. Decision 4: `docs/64` Q5's recommendation is **no**, the tenant-crossing danger is **measured**, and it does not block F-7. Also corrects `docs/87` §E — the **attempt store** is required and is where Decision 5's counters live |
| **docs/89** | **Decision record: attempt store, site authority, and the Decision 5 gate.** **CLOSED:** the separate **non-tenant attempt store** (AS-1…AS-6 — never the raw code, `code_prefix` + `code_hash`, outcome classified, counters live here, not `mt_audit_log`, and **no tenant data may be required for the anti-enumeration check**, because an unknown code resolves no customer); and **site authority** — `voucher.site_id` → authorized `dnb_site_nas` → decision, with invariant **SI-1: changing `nas_claimed` alone must never change the site a voucher is authorized at**. **`guest` is not added to `actor_kind`** — the attempt store is the second of the two options `docs/64` §10 offered, and no actor kind is **not** no attribution. **Decision 5 REMAINS OPEN** behind an explicit gate; its eight parameters are restated with **evidence still required**, and read **UNMEASURED / OPEN** wherever a number would be invented |
| **docs/90** | **The MikroTik plugin boundary and the two-plane Admin Panel (built, dev/test).** The control plane becomes an **installable unit**: a manifest whose declared surface is asserted equal to the served surface, a lifecycle that verifies its own work, and **the API entry point that did not previously exist** — `AdminRoutes` was reachable only from tests. The panel moves out of `public/admin/` to `panel/` as a separate consumer holding no database access. Admin UI restructured into **Network plane / Commercial plane**, with Router Detail, a provisioning ladder, Network health and Diagnostics. **Measured: there is no source for WAN link, WireGuard, RADIUS or HotSpot status, and `mt_devices.last_seen_at` is never written** — so all five render "no signal" with reasons, never green. All four router actions render inert with specific reasons. Seven POST paths exist and each answers 501. 1,349 assertions |
| **docs/91** | **MikroTik Admin prototype v0.1 — a coherent simulated estate (built, dev/test).** Diagnoses the list/detail contradiction: there was **no simulator dataset**, the panel read `dnb_test` which the suite drops every run, so it displayed fixture debris — and the two screenshots were never one state. The simulator now has **its own database**, builds through the **real write paths** (so the audit trail exists because the acts happened), and marks every identifier `SIM-`. Router Detail gains IDENTITY / CONNECTIVITY / PROVISIONING / OPERATIONS / ACTIONS with names resolved instead of truncated uuids. **Three further gaps found:** sessions are **not attributable to a router** (`mt_session_account` never sets `device_id`), uplink is **measured but not Admin-readable** (D-4 open), and `wan_interface_set_at` is unexposed. The suite caught the simulator breaking **three architectural rules** (inspector in `src/`, entitlement write, octets without gigawords). 1,446 assertions |
| **docs/92** | **v0.2 inventory — HotSpot, Vouchers, Sessions (inventory only, nothing built).** Read from the running schema, not from memory. Most v0.2 business logic **already exists**; what is missing is read exposure. **Three genuine blockers:** there is **no `mt_admin_services()`**, so HotSpot service state has no Admin read path; there is **no single-row voucher projection**, so voucher detail cannot be built; and **two of the four voucher lifecycle states are unreachable** — `activating` is not in the CHECK (F-7, unauthorized) and **nothing anywhere writes `expired`** (measured: the only state write in the codebase is `revoke`). Also: the voucher **code is withheld from every Admin projection by design** (Decision 3), and sessions carry **no username and no site** (site is derivable via the voucher). Work split into UI-only, needs-approval, and blocked |
| **docs/93** | **v0.2 — HotSpot, Vouchers, Sessions (built, dev/test).** Migration **021** adds the two approved projections and nothing else: **`mt_admin_services()`** (whose `status` is **recorded, not observed** — a test asserts HotSpot liveness stays `UNMEASURED` whatever it returns) and **`mt_admin_voucher(uuid)`** (column list **identical to the list**, so a detail view cannot reach a withheld field one row at a time; `code` withheld). The read boundary goes from eleven to **thirteen**. Voucher lifecycle is declared **server-side**: two states reachable, three not, each saying what is missing — and **no `active` or `expired` voucher was manufactured**. Operator/engineer split: Router Detail shows concise verdicts, Diagnostics carries the evidence. `WAN interface` becomes **`WAN interface assignment — RECORDED`**. 1,488 assertions |
| **docs/94** | **v0.3 Admin surface inventory (inventory only, nothing built).** Fourteen destinations classified A–E. **Two findings outrank any single screen: every one of the nine Admin list projections is UNBOUNDED** — no `LIMIT`, no pagination, no server-side filter, while the customer-facing services *are* bounded — and **seven of fourteen screens are flat unfiltered lists**, because the shared `list()` helper offers no search or filter at all. Confirms HotSpot should become **index + detail**, and records that *"needs attention"* has no honest definition while RADIUS and HotSpot state are `NOT MEASURED`. Finds **`mt_admin_customer(uuid)` and `api.customer(id)` both built and unused** — Customer Detail is the cheapest gain. Notes there is **no operational workflow screen and cannot be one** until a write route is bound. Bounding the projections is tabled as the next boundary decision |
| **docs/95** | **Admin list read contract — decision packet (nothing chosen).** Covers **ten** list projections, not nine: `mt_admin_voucher_batches()` is one too. Three measured constraints: **eight of ten order by a non-unique column**, and **vouchers are already degenerate** — 17 rows, **3 distinct `created_at`**, because `issueBatch` writes a batch in one transaction and `now()` is transaction time, so an 8-row batch is one tie group with arbitrary internal order; **not one index serves an Admin list order** (`EXPLAIN` shows Seq Scan + Sort — every index is tenant-leading because the *customer* API was the design target); and **the house has "bounded", never "paginated"**, so there is no cursor format or page-size precedent to follow. Records that **bounding the lists silently breaks every count the panel shows**. Nine decisions **A-1…A-9** are tabled unchosen; **A-7 (stable ordering) and A-9 (indexes) are the load-bearing pair**. **D: none of this work is hardware-dependent** — it can complete while F6-B stays closed |
| **docs/96** | **Installability audit — INSTALLABLE WITH CONDITIONS.** Classified **B, an independent Domain-B service**, measurably **not** a UCRM plugin; before it, **D** — a project with no artifact. 67-check disposable test: install → serve → simulate → uninstall with zero residue, then a second install from the artifact alone. |
| **docs/97** | **B-1 CLOSED — no credential in the repository.** Migrations declare privilege, the installer supplies credentials. Six `PASSWORD '<literal>'` clauses removed; `rolpassword` NULL until install provisions one. Five defects found by running it, including a refusal that fired *after* the credentials were applied. |
| **docs/98** | **UISP/uCRM integration audit.** Plugin contract MEASURED from two plugins already running on the DishNet server: ZIP with root `manifest.json`, `main.php` on a ~5-minute tick, **one** public file, `ucrm.json` → `pluginAppKey` → `api/v1.0/*`, staff identity via `/current-user`, storage SQLite. **RC1 is NOT installable that way and must not be forced.** **I-1: Domain B is an unlinked second customer master.** |
| **docs/99** | **Customer identity — two models, neither chosen.** Model A (uCRM source of truth) vs Model B (entity + link). **I-2**: `mt_principals` duplicates uCRM contact data. Superseded in part by `docs/101`. |
| **docs/100** | **Identity census — READ-ONLY, development databases. NOT E-2.** Proved by execution that an existing customer **cannot be deleted**; 27 FKs; **`mt_principals.phone` is the authentication key**; `credential_hash` dead; uCRM **does** expose stable service ids. Operator questions reduced 13 → 4. |
| **docs/101** | **Q5 = C (both workflows). L-1 CLOSED.** Equipment-first is `device.customer_id = NULL` — no placeholder customer. **Determined for approval:** customer link **1:1**, service link **1:1**, **columns not a link table**, service identity is the uCRM **service id**, **`UNCLAIMED` is a predicate not a state**, writes `dnb_adminwrite`-only, plus a **coherence rule** no FK can express. Measured: unclaimed devices already invisible to every tenant. |
| **docs/102** | **The commercial identity boundary, measured.** Every operation classified. **The effective boundary is RLS + triggers + functions, NOT grants** (proved by execution). **`mt_services`/`mt_sites` have no production writer.** **No single gate exists** — site and voucher reference no device. Redemption must **never** consult uCRM; audit must **never** be gated. **B-2**: the customer-plane commercial writes are not behind definer functions. |

Reproducible artifacts live in `dishnet-mikrotik-control-plane/tools/audit/`:
`proto_f6*` (the actor and lifecycle prototypes), `f6_radius_restriction.sh`
with `f6_rad_client.py` (the isolated FreeRADIUS experiment), and
`f6_cb_sql_dynamic.sh` (candidate C-b), `f6_site_bound_reassign.sh` (the
site-keyed mapping, reassignment, and the fail-closed cases S1–S9) and
`f6_huntgroup_reassign.sh` (E10). All disposable; none touches Phase 0.

---

## Production state

Nothing production-facing has been changed by any of the above: no code, schema,
privilege, FreeRADIUS configuration or deployment, on either installation. F6
itself remains unfixed — `dnb_app` still holds `EXECUTE` on `mt_voucher_redeem`,
as docs/63 recorded it.

**This is true after Decision 2a closed.** Closing 2a chose a mechanism; it built
nothing. `dnb_cred_site` and `dnb_site_nas` do not exist, the production
authorize query is still username-only, and no new role or privilege has been
created.

---

## Where the work stands

| | |
|---|---|
| **Closed** | Decisions **1**, **2a**, **2b**, **3**, **7**. F1–F13 frozen |
| **Open** | Decision **5** (rate-limit and timing numbers) — **gated**, see `docs/89` §3.2; Decision **6** (retention). Decision **4** (front-desk activation): **no second redemption path required** (`docs/88` Part B) |
| **The next gate** | **The operator runs the census — docs/79 is the handoff.** This session cannot (verified access limitation, docs/78 §1.1) and will not probe further. Then: review evidence → decide B → finalize backfill/migration → explicit F6 authorization |
| **Voucher/AAA path** | **NON-CONFORMING — NOT PRODUCTION CAPABLE** (`docs/86`, remediation designed in `docs/87`). The implementation drifted from Decisions 1 and 3 before production. No deployment occurred |
| **Admin prototype** | v0.2 built (`docs/93`): HotSpot, voucher detail, richer sessions, operator/engineer split, thirteen projections. v0.1 (`docs/91`): a coherent `SIM-` estate in its own database, five-section Router Detail, three new honesty gaps recorded. No gate moved |
| **Plugin boundary** | Built (`docs/90`). Installable manifest, lifecycle, API entry point; panel separated as a consumer; two-plane Admin UI. No gate moved |
| **Installability** | **INSTALLABLE WITH CONDITIONS** (`docs/96`). It is **B — an independent Domain-B service**, measurably **not** a UCRM plugin; before this it was **D**, a project with no artifact. A 90-file tarball, a preflight, a bootstrap step and a 32-check disposable install test now exist, proven install → serve → simulate → uninstall with **zero residue**. **B-1 is CLOSED** (`docs/97`): the migrations create every login role with no password, the installer provisions one at install time, and 0 of 6 burned credentials authenticate on a cluster that enforces scram. **B-2 remains OPEN** — no staff authentication (W-4), so the panel renders only under the development identity. Release candidate `0.1.0-rc1`, content digest `aa48b4b4…b1db63c7`, 67-check disposable test. **Not installed anywhere.** No gate moved |
| **F6-A (application)** | Building. Admin **read** boundary and read-only Admin UI done; the Admin **write** floor (W-1/W-2/W-3) is implemented in the development schema with **no write route bound**. `DenyAllIdentity` is still the production Admin binding (W-4 open) |
| **F6** | **NOT AUTHORIZED.** It is not the next step, and closing 2a did not bring it closer to being authorized |
| **UISP/uCRM integration** | **AUDITED, nothing decided** (`docs/98`). The plugin contract is MEASURED from two plugins already running on that server: ZIP with root `manifest.json`, `main.php` on a ~5-minute tick, **one** public file (`public.php?page=`), `ucrm.json` → `pluginAppKey` → `api/v1.0/*` with `X-Auth-App-Key`, staff identity by forwarding the uCRM session cookie to `/current-user`, storage = data dir + SQLite. **RC1 is NOT installable through it and must not be forced**: every Domain-B control lives in PostgreSQL features a plugin cannot have. Recommendation **R-1 — a thin bridge plugin, Domain B stays a separate service**. **I-1: Domain B is today a second, unlinked customer master** — `ucrm_client_id`'s only writer is a test fixture. **U-1…U-5 must be decided first** |
| **Customer identity model** | **DESIGNED, NOT CHOSEN** (`docs/99`). Model A (uCRM client is the source of truth, `mt_customers` a projection, `ucrm_client_id NOT NULL`) and Model B (Domain-B entity carrying a linked `ucrm_client_id` with provenance) are compared factually; **neither is recommended**. **U-2 cannot be closed by choosing a value — it needs the production census.** New findings: **I-2**, `mt_principals` duplicates uCRM's name/phone/email; **`mt_services` has no uCRM service reference at all**; and the estate's own precedent (`026_lte_financial_ledger.sql`) is a link table with `linked_by`. The staff trust boundary is **U-7: S-1 proxy or S-2 signed assertion** — Domain B must never take a staff identity from a browser |
| **Identity census** | **READ-ONLY, development databases — NOT E-2** (`docs/100`). Production state stays unmeasured. Schema truths that generalise: an existing customer **cannot be deleted** (proved by execution); **`mt_principals.phone` is the authentication key**; deleting a principal **silently nulls `sold_by`/`created_by`**; uCRM exposes stable service ids Domain B cannot represent |
| **Q5 — CLOSED: C, BOTH; L-1 CLOSED** | Customer-first **and** equipment-first (`docs/101`). Equipment-first is a **device with no customer**, already built — never a placeholder customer. Both converge at `mt_device_assign` |
| **Commercial boundary** | **MEASURED, nothing built** (`docs/102`). **No single gate** — `service → site → plan → voucher` completes a sale with no router. The rule is enforced at a **set** of operations, never at `mt_customer_create`. **B-2**: plans and vouchers write directly under RLS, so gating them means definer functions with audit — the largest item U-1 implies |
| **Write paths** | **MEASURED** (`docs/103`). **Four of the five identity entities have NO production write path**: `mt_customers` has a function but no caller, and `mt_principals`/`mt_services`/`mt_sites` are written **only by `Plugin/Simulator.php`** — which the release package excludes, so an installed RC1 cannot create a principal, service or site at all. `src/Customers/` is an **empty directory**. 35 operations inventoried with audit and idempotency status; **`POST /me/vouchers` is the only idempotent route**. Two project rules recorded: the **security evidence hierarchy** (grants are the weakest evidence, never proof) and **repository-edit safety** (an edit must assert anchor, count and result; exit 0 is not evidence) |
| **Onboarding spine** | **DESIGNED, nothing built** (`docs/104`). The four missing writers are **one lifecycle**, not four tasks: customer caller → principal → service → site. Cardinality read off the schema — **`mt_principals.phone` is unique GLOBALLY**, `mt_services.kind` permits exactly one value, and the **minimum valid customer is a name and an actor** (no uCRM link, service, site, router or phone). **Steps 1–6 produce a commercially active customer with no hardware**, which is why the uCRM link cannot be gated on device assignment. New defect **O-1**: `mt_sites` has two independent FKs, so a tenant can attach its site to **another customer's service** — proved by execution as `dnb_app` under RLS. No disclosure (RLS still hides the service row), but a confirmed integrity defect and **cross-tenant denial**: the victim can never end that service. Latent only because no production writer creates a site — **the spine must close it before building one**. **U-1 deliberately NOT closed** |
| **Identity integrity** | **DESIGNED, not authorized** (`docs/105`). **O-1 fully characterised under controls**: B deletes its own unreferenced service (`DELETE 1`, control) → A attaches its site to B's service by literal UUID (`INSERT 0 1`) → **A cannot read that service (0)** and **B cannot see the referencing row (0)** → B's delete is now refused. **Referential integrity is enforced BELOW RLS** — neither party can see the other's row, yet the constraint binds both, which is why a composite FK is a floor and an application check is not. **Not disclosure, not enumerable** (the attacker must name a `gen_random_uuid()` it cannot read); the realistic trigger is a writer that passes a `service_id` it did not derive — **exactly what the spine will build**. Remediation is W-2's shape one level up: `UNIQUE (id, customer_id)` on `mt_services` + composite FK, both existing FKs kept, **no CHECK** (both columns are already `NOT NULL`), `ON UPDATE NO ACTION` — which makes service migration impossible by accident. **The census needs no superuser**: `dnb_def_admin`'s SELECT-only `USING true` policies and `mt_admin_sites()`/`mt_admin_services()` already expose every column O-1 concerns, EXECUTE-able by `dnb_adminapi`. **P-A RETIRED — it is C6/C16**, open since `docs/47`. **I-A: the existing idempotency mechanism is unusable by all four writers** — keyed by a `customer_id` that does not exist yet, and `dnb_adminwrite` holds zero table privileges. **U-1 still open**; boundary proposed at `mt_service_create` |
| **Remediation package** | **DEMONSTRATED, not applied** (`docs/106`). Every DDL statement was executed against the real schema **inside a rolled-back transaction** — residue zero rows, zero constraints. `ALTER TABLE mt_sites ADD CONSTRAINT mt_sites_service_customer_fkey FOREIGN KEY (customer_id, service_id) REFERENCES mt_services (customer_id, id)`, plus `UNIQUE (id, customer_id)` on `mt_services`. **Referenced column order is cosmetic** — both orders accepted; PostgreSQL matches the column *set*. **The UNIQUE cannot fail on existing data** (PK(id) is strictly stronger — the duplicate is rejected by `svc_pkey`), so **only the FK can refuse**. **No MATCH FULL and no CHECK**: both `mt_sites` columns are already `NOT NULL`, so MATCH SIMPLE is equivalent — unlike `mt_devices`, whose nullable `site_id` is why W-2 needed one. **The migration fails closed by itself**, but its error names only ONE offending pair, which is why the census enumerates. **Locks, per statement:** the UNIQUE takes **ACCESS EXCLUSIVE on `mt_services`** (the only read-blocking window); the FK takes SHARE ROW EXCLUSIVE on both, so **`mt_sites` reads are never blocked**. Matrix measured against the proposed schema: own-service INSERT passes, cross-tenant INSERT and UPDATE both refused, cross-reads 0 with non-zero controls. **P-B is BREAKING** — `mt_auth_issue_code` uses a non-STRICT `SELECT … INTO` with no ORDER BY, measured to return the first row silently, so relaxing phone uniqueness would bind an OTP to a **non-deterministic tenant**; whether one person may hold two customers is **C10**, open. **P-C recommended FORBIDDEN** — `mt_auth_sessions` carries its own `customer_id` and `mt_auth_resolve_token` returns the session's copy, so reassignment would leave live sessions serving the old tenant. **U-1 refined, still open** |
| **Identity decisions** | **FOUR CLOSED, U-1 reduced to one question** (`docs/107`). **P-B CLOSED — `mt_principals.phone` stays globally UNIQUE**; `mt_auth_issue_code`'s non-STRICT `SELECT … INTO` would otherwise bind a one-time code, and the session's tenant, to an arbitrary principal. **C10 must not be solved by weakening it.** **P-C CLOSED — principal reassignment PROHIBITED**: resolution re-reads `p.status` live but takes `customer_id` from the session's snapshot, so disable takes effect immediately and reassignment would not take effect at all. Lifecycle is **disable + create new**. **S-A CLOSED — service migration PROHIBITED as an ordinary operation**; O-1 plus `ON UPDATE NO ACTION` turns the policy into a constraint. Ownership change is **end the service, create a new one**. **O-1 closed as a design**, with the census and the migration as **two separate operational gates** — a script that measures and then acts on its own measurement gives the operator nothing to approve. **U-1/U-5 OPEN**, reduced to **Q7**: does the uCRM service always exist before the Domain-B service? Gating at creation costs no schema change; gating at activation needs a new `mt_services.status` value, which is a schema decision. **I-A designed**: both existing mechanisms are tenant-scoped (`mt_idempotency` and `mt_intents_idem_uq` alike), so the spine needs a non-tenant store — three mechanisms across eight operations. **Convergence is `mt_device_assign`, and convergence is not a gate.** RC1 still must NOT be installed into production |
| **Onboarding spine / idempotency** | **DESIGNED; one correction, one new blocker** (`docs/108`). **Correction:** `bin/worker.php` binds **`NullDelivery`** — `RouterOsDelivery` is not referenced by `Bindings` at all — so **no intent reaches a router today**; `docs/107`'s "reaches a router" is **withdrawn**. The intent split is **THREE-way**: `voucher.publish` safe by key; **`voucher.revoke` safe *incidentally*** — `UPDATE … WHERE id = ? AND state IN ('unused','active')` makes the handler answer 404 **before** the enqueue, so safety depends on statement order, not design; **`session.disconnect` unguarded — NEW BLOCKER**, duplicate intent + duplicate audit row, **must be fixed before F6-B**. **RULE I-1:** detection must precede the audited mutation, because W-1 writes the audit row inside the function and `mt_audit_log` is append-only — **a duplicate audit row is a false record that cannot be corrected**. **Seven of eight onboarding operations have no tenant context at execution**, so a non-tenant store is required for the four with no natural key, while a unique constraint suffices — and is stronger — for the rest. **Q7 searched for and NOT answered**: the sibling plugin's `lte_service_links` shows a local entity existing unlinked, but it links a uCRM **client**, not a service, in a different product line. **U-1/U-5 stay OPEN, and Q7 is the critical path — not O-1** |
| **Q7 evidence search** | **Q7 NOT ANSWERED; U-1 and U-5 stay OPEN** (`docs/109`). Searched operational sources, not schema. **F-1** the deployed Uganda sales assistant states its own scope — *"We sell **Starlink** … We do NOT sell fiber"* — **HotSpot is not in the sales flow**. **F-2** the only `MikroTik` string in the live stack is a vendor name in a hardware-advice list; **no HotSpot revenue path exists**. **F-3** no uCRM service plan names HotSpot or MikroTik anywhere. **F-4** `hotspot.html` mentions vouchers and has three *contact* calls-to-action and **no price** — a **lead-generation page, not a product**. **F-5** the site README records its own caveat: *"If any of these is not actually sold in Uganda yet…"* — **the repository does not know whether HotSpot is sold**. Therefore **Q7 is not an archaeological question**: there is no onboarding history to recover, because the product is at the enquiry stage and the platform does not exist. Reduced to **one uCRM lookup the operator can do in a single screen** — *does a HotSpot service plan exist, and does any client hold one?* **U-5's premise may be empty** if no such plan exists. Also flagged, unresolved: `docs/107` §9.2 proposed voucher issuance require the uCRM link, which diverges from the stated boundary — a **stored-link precondition** is not a **runtime dependency**, but it is still a prerequisite, and that is a business decision |
| **Standalone boundary** | **FROZEN; one proposal withdrawn** (`docs/110`). **`docs/105` §9.2 / `docs/106` §9 are WITHDRAWN**: a stored uCRM link is **not** a prerequisite for plan or voucher operations. The error was treating **commercial representation** and **technical capability** as one requirement — a voucher sale is revenue to the **operator**, and becomes DishNet revenue only where DishNet bills that operator. **The boundary is MEASURED, not designed:** Domain B contains **zero** non-column uCRM references, `ucrm_client_id`'s only writer is still `tests/bootstrap.php`, and the **only** outbound HTTP client in Domain B is `RouterOs/RestClient.php` — to a router, not to uCRM. **So the standalone boundary needs no migration**: `ucrm_client_id` is already nullable, and the one decision that would break it is **U-2**, still gated on E-2. uCRM enters at **exactly one point** — an audited link against an existing operator — and is **NOT in the real-time path** `redemption → AAA → RADIUS → session → accounting`, which is a **security** requirement (anti-enumeration, response uniformity, availability, privilege), not a preference. **If uCRM is down, every HotSpot operation continues**; billing, support and — under the proposed bridge — **Admin staff auth** do not. **U-1 restated as a commercial question**, U-5 deferred, no guest CRM record, no placeholder customer |
| **uCRM bridge** | **DESIGNED, not authorized; three new blockers** (`docs/111`). The bridge uses **Domain-B APIs only — never its PostgreSQL**, holds no Domain-B role credential, and is **never a dependency of guest redemption**. Measured surface: **15 GET routes, `surface: read-only`, and 7 declared-unbound POSTs that each answer 501**. **N-1 — the bridge's two required endpoints (create operator, link to uCRM) DO NOT EXIST, not even as 501s**, so **the bridge depends on the onboarding spine**, not the reverse. **N-2 — there is no way for a bridge to authenticate today**: `DenyAllIdentity` answers 401 on every route (W-4). **N-3 — if S-2 is chosen its signing key is a new secret**, inheriting every B-1 lesson. **What it can never reach, measured:** `dnb_def_admin` holds **NO policy** on `mt_device_secrets`, `mt_hotspot_users`, `mt_auth_sessions` or `mt_auth_codes` — so the Admin boundary **structurally cannot read a secret-bearing table**; `wg_pubkey` is absent from `mt_admin_router()` and `code` from both voucher projections; RADIUS credentials are in a **separate instance** the Admin API cannot reach. **Cardinality is OPEN and cuts both ways** — `ucrm_client_id UNIQUE` **already enforces 1:1**, so anything else is a schema change, while the working precedent is a pair-unique link table; **decide C10 BEFORE cardinality**. Write surface is **at most two operations**, neither a network operation — a CRM plugin has no business rebooting a router. **Fail closed on identity, fail soft on data** |
| **Spine final design** | **DESIGNED; one new finding, A-1** (`docs/112`). All nine operations specified with actor, preconditions, dependencies, idempotency, audit and status. **A-1 extends `docs/84` F-3, proved by execution:** F-3 called it hypothetical, but `dnb_admin` **can INSERT into `mt_audit_log`** — **the audit trail is forgeable**, and `mt_audit_log` is append-only, so a forged row **cannot be removed**. `dnb_worker` holds the same grant (migration 015 line 73 grants both; F-3 names only `dnb_admin`). **RLS still bounds it to the caller's own tenant** — the customer INSERT was refused — so it is an **attribution/integrity** issue, **not** a tenancy breach, and it is **latent**: no production route connects as `dnb_admin`. **Therefore none of the nine may be EXECUTE-able by `dnb_admin`, and no new route may connect as it.** Also recorded: `AdminRoutes.php:103` says *"dnb_admin holds EXECUTE and no table privilege whatsoever"* — true of `dnb_adminapi`, which the route uses, **false of the role it names**; behaviour correct, comment wrong. **Required vs optional settled:** principal is needed **for a login only**; device is needed for **nothing in the commercial chain**; **uCRM for nothing**. **`session.disconnect` deliberately NOT fixed here** |
| **A-1 remediation** | **DESIGNED, evidence measured; T1 needs no schema change** (`docs/113`). Every login role execution-tested with a connection control. **THREE roles can forge an audit row — `dnb_admin`, `dnb_app`, `dnb_worker`** — not two: **`dnb_app` is the third, and it is the customer-facing HTTP role**, which writes six audit sites today (F-8). **UPDATE and DELETE are refused for every role, including those holding the grant** — the append-only trigger holds universally, so the exposure is **forgery only, never tampering or erasure**. **Diagnosis:** migration 015's blanket grant rested on *"each is subject to RLS"*, which holds for business tables but **fails for `mt_audit_log`, because RLS constrains which tenant a row belongs to — not whether it is true**. **`dnb_admin` has NO production caller**: the only caller of `Database::admin()` is `Simulator.php`, which is excluded from the package — so T1 breaks the simulator and nothing else. **`dnb_worker` genuinely needs `mt_intents`** but not audit. **PROVEN the remediation is safe:** `dnb_adminwrite` cannot INSERT or even SELECT `mt_audit_log`, yet `mt_customer_create` succeeds and **writes 1 audit row** — the eight functions depend on `dnb_def_audit`'s privilege, never the caller's. **Revoking the grant without revoking ALTER DEFAULT PRIVILEGES is a fix that expires.** Also found: **`dnb_plain`, a stray role nothing in the repo creates** — the census must enumerate roles |
| **A-1 / T1 — IMPLEMENTED** | **Migration 022 shipped** (dev/test only). `REVOKE INSERT ON mt_audit_log FROM dnb_admin` + `ALTER DEFAULT PRIVILEGES … REVOKE INSERT … FROM dnb_admin`, with `tests/test_audit_boundary.php`. Suite **28 suites / 1,617 assertions / 0 failed** (was 27 / 1,599). **Scope is dnb_admin ONLY, measured before writing:** its sole caller is `Simulator.php`, which **only SELECTs** `mt_audit_log` — so **`docs/113`'s prediction that T1 breaks the simulator is WRONG for a targeted revoke; it breaks nothing.** `dnb_app` (six `Routes.php` sites via `Database::app()`) and `dnb_worker` (`IntentWorker` lines 63/77 via `Database::worker()`) **would break a live path**, so T2/T3 stay open until each has a controlled path. **The default privilege was the half that would have expired** — the next table created would have re-granted it, and the attempt store and idempotency store are both still to come. **A-2 is asserted, not hidden:** the suite asserts `dnb_app` and `dnb_worker` STILL hold audit INSERT, so finishing T2/T3 must break those lines. Controls: `dnb_adminwrite` holds no audit privilege yet writes exactly 1 audit row via `dnb_def_audit`; roles enumerated from `pg_roles`; and the **control on the control** — reverting the grant makes the identical statement return `INSERT 0 1`. **A-1 is NOT closed: T1 is implemented in development; production is a separate gate behind the census** |
| **A-1 / T2 — IMPLEMENTED; T3 — STOPPED AND REPORTED** | **Migration 023 shipped** (dev/test only). **T2:** `IntentWorker`'s two audit sites (`intent.confirmed`, `intent.failed`) now call **`mt_intent_audit()`**, a `SECURITY DEFINER` owned by **`dnb_def_work`** — the role that *already* owns the intent lifecycle and its `mt_intents` policies, so no boundary was invented. Then `REVOKE INSERT ON mt_audit_log FROM dnb_worker` **plus the matching `ALTER DEFAULT PRIVILEGES`**. **The function removes three caller choices:** `customer_id` is **DERIVED from the intent**, `actor_kind` is **fixed to `system`**, `action` is **constrained to two values**; only the worker's own identity remains a parameter, which is the W-1 shape. `dnb_worker` **keeps `mt_intents` UPDATE** — the claim/lease is not audit, and a test proves `mt_intent_claim` still runs. **T3: STOPPED, not done, and the reason is measured** — `dnb_app` may EXECUTE exactly **six** definer functions and **none performs any of its six audited mutations** (five `mt_auth_*`, whose routes audit nothing; plus `mt_voucher_redeem`, which has no caller and is to be deleted). `PlanRepository`, `VoucherService` and `IntentQueue` write raw DML under RLS. **Granting `dnb_app` EXECUTE on `mt_audit_write` is NOT a fix:** of `mt_audit_log`'s ten columns **only `id` and `at` are not caller parameters**, so it is **exactly as forgeable as INSERT** — relocating the forgery while looking remediated. Building those six functions **is B-2**. Also measured: **even the schema owner cannot GRANT that EXECUTE** — the function is `dnb_def_audit`'s. **Controls:** the revoke is proved **by execution**, with the grant restored (same statement inserts) and withdrawn again, leaving no forged row. Suite **28 suites / 1,641 assertions / 0 failed** (1,617 → 1,641). **A-1 is still NOT closed: `dnb_app` remains the one role that can forge an audit row**, asserted in the suite so closing B-2/T3 breaks it |
| **A-1 / T3 + B-2 — IMPLEMENTED; A-1 CLOSED in development** | **Migration 024 shipped** (dev/test only). Six `SECURITY DEFINER` functions, one per customer-plane mutation — `mt_plan_create` · `mt_plan_update` · `mt_plan_retire` · `mt_voucher_batch_issue` · `mt_voucher_revoke` · `mt_session_disconnect_request` — each performing the write AND its audit row in one transaction, then `REVOKE INSERT ON mt_audit_log FROM dnb_app` plus the matching `ALTER DEFAULT PRIVILEGES`. **No login role can write an audit row any more.** **`dnb_def_comm` is a NEW definer role and the reason is measured:** every existing definer role already carries a widening `USING (true)` policy on exactly the table these functions write (`dnb_def_net` on `mt_vouchers`/`mt_hotspot_users`, `dnb_def_work` on `mt_intents`, `dnb_def_admin` on all), so reusing one would have REMOVED the isolation. **Tenancy is not enforced by the function bodies** — `dnb_def_comm` has no policy of its own and inherits `<table>_isolation FOR ALL TO public`; **proved by execution: a widening policy breaks 11 assertions, removing it restores all 89.** **No `p_customer` parameter exists in any of the six**, so a forged customer is unrepresentable rather than rejected; the actor is a parameter (W-1) verified against the caller's own visibility. **`src/Audit/AuditLog.php` DELETED** — after the revoke it could only fail. **Behaviour reproduced, not redesigned:** the `mt_hotspot_users` row is still written at issue with the reversible username (docs/86, carried over; **no AAA publication reintroduced** — Decision 1 stays Model B), a NULL update field still means unchanged, `plan.retire` still has no state guard, and an idempotency key still deduplicates only the intent. **`session.disconnect` replay deliberately NOT fixed and the gap is ASSERTED** — still a blocker before F6-B. **What T3 did NOT close, also asserted:** `dnb_app` retains direct INSERT/UPDATE/DELETE on the five business tables (migration 015's blanket grant), so the audit trail cannot be forged but an UNAUDITED direct mutation is still possible — the remaining half of B-2. Suite **29 suites / 1,740 assertions / 0 failed**, stable over two runs (1,641 → 1,740). **A-1 is closed in DEVELOPMENT ONLY; production application remains behind the census** |
| **O-1 acceptance (SYNTHETIC) — `docs/106`'s fail-closed claim WITHDRAWN** | `tools/audit/o1_acceptance.php`, 52 assertions, throwaway `dnb_o1acc`, `O1FIX-` prefixed, dropped at the end — **NOT production evidence; production stays CENSUS NOT OBTAINED.** **THE FINDING, proved by execution against a violating estate:** as the owner `dnb` the O-1 migration applies with **no error** and marks the FK **`convalidated = true`** while the violating row remains — because since 017 (F2) the owner is not a superuser and `mt_sites` has FORCE RLS, so with no tenant context it sees **zero rows** and the validation scan finds nothing. A role that sees every row **refuses**, naming the constraint and the offending pair, 0 constraints left. **Fix: `SET LOCAL row_security = off;`** — **not a way of bypassing RLS and not to be described as one**: the property is that **the migration refuses to proceed when its own validation query would be affected by row-level security**, rather than validating whatever subset was visible. It fails closed and gains sight of nothing. **`docs/106` §6 is corrected AT SOURCE.** The migration is a SEPARATE act from the census — `census → looks clean → ALTER TABLE` is the sequence this rules out; `docs/79` §7b carries the six-step operator sequence whose step 6 is **independent verification afterwards**, because exit code 0 is now known not to be evidence that the invariant holds. **This makes the census load-bearing, not merely useful:** the migration cannot be relied on to catch a violation and its error names only one pair. **Five of fourteen anomalies cannot exist at level 25** (orphan customer, orphan service, NULL service_id, device mismatch, duplicate tunnel_ip) — each proved refused by the current schema first, then detected with that single guard suppressed in the throwaway database. **Census corrected:** `BLOCKED(n)` counted detectors not rows (one bad row read BLOCKED(3)); and the harness's two undeclared env vars were rejected by `test_installability.php`, which sweeps `tools/` — host/port now come from the declared `DNB_DSN`. The candidate DDL lives in `tools/`, **deliberately not in `migrations/`**. Suite unchanged at **29 / 1,763 / 0**; zero residue |
| **B-2 second half — IMPLEMENTED; dnb_app is READ-ONLY on the commercial plane** | **Migration 025 shipped** (dev/test only). T3 closed audit *forgery*; this closes *unaudited mutation*. Migration 006 line 47 had granted `dnb_app` SELECT/INSERT/UPDATE/DELETE on ALL TABLES, so the application could still bypass the six functions. **Every `dnb_app` write path was inventoried from the code first**, and all six were already replaced by 024 (`mt_plans`←PlanRepository, `mt_vouchers`/`mt_voucher_batches`/`mt_hotspot_users`←VoucherService, `mt_intents`←three enqueue sites, `mt_profiles`←ProfileResolver). INSERT/UPDATE/DELETE revoked on all six; **SELECT deliberately kept** (list/find/forCustomer are ordinary RLS-scoped reads). `dnb_worker` keeps `mt_intents` UPDATE — the claim/lease is its own. **`ProfileResolver` DELETED** (no caller since 024). **Default privileges closed too** — 006 line 50 would have handed the writes back on the next table; 024 took INSERT, 025 takes UPDATE and DELETE, SELECT stays. The simulator's one `dnb_app` enqueue moved to the admin connection (a provisioning job is network-plane). **Tests refactored, not privileges preserved:** 32 fixture writes moved to `Database::inspector()`, the documented fixture identity. **Two traps recorded:** read-isolation assertions briefly moved to that BYPASSRLS identity would have gone VACUOUS and were put back on `dnb_app`; and the default-privilege probe must create its table as the OWNER, because ALTER DEFAULT PRIVILEGES is recorded per granting role. The two no-delete tests are now two assertions — fixture identity proves the TRIGGER, `dnb_app` proves the privilege. **Proved substantive: widening every `*_isolation` policy to USING(true) breaks 84 assertions across five suites.** Still open and asserted: `dnb_app` keeps UPDATE/DELETE *grants* on `mt_audit_log`, refused by the append-only trigger — the clearest demonstration that a grant was never the boundary. Suite **29 suites / 1,763 assertions / 0 failed**, stable over two runs. **Development only; production remains behind the census** |
| **DishNet Portal — staff authentication PLANNED** | **PLAN, nothing implemented** (`docs/114`). Direction corrected: the product is the **DishNet Portal** (DishNet Admin + Customer PWA over the Domain-B API); **uCRM / UISP / Splynx are not the operating portal and not a staff-auth dependency** — `docs/111`'s staff-identity path (U-7, S-1/S-2) is **superseded**. **W-4 is authorised to be designed**: a DishNet-owned provider — `mt_staff` + `mt_staff_sessions` owned by a new NOLOGIN `dnb_def_staff`, bcrypt and TOTP verified **inside PostgreSQL** (pgcrypto, the schema's first extension), decaying lockout inside `mt_staff_login()`, opaque HttpOnly/Secure/SameSite=Strict cookie stored hashed under a label-derived key, revocable server-side sessions, actor = username via W-1, `mt_current_customer()` never set on the Admin plane. Sections A–K; gates G-A…G-F; decisions D-AUTH-1…7 open. `DenyAllIdentity` remains the production binding; migrations end at 025; no gate moved |
| **Multi-operator tenancy — ANALYSED, not decided** | **ANALYSIS, no schema change** (`docs/115`). A reference ISP portal (operating model only) shows ISP → Operator → Location → Subscriber. Measured on Domain B: `mt_customers` **already is the Operator** — nothing references anything above it, sites/routers/plans/vouchers/sessions all hang off it, 18 FORCED-RLS policies key on it, `docs/110` names it "the Domain-B operator". A table above it would re-key 18 tables, 18 policies, 32 functions and the session model to represent subscriber accounts the product does not have (F11). **Recommended: vocabulary at the API/UI, no tenant layer.** Operator Staff = `mt_principals` (capability model undefined, C6/C16); Guest = no identity. Twelve open decisions T-1…T-12; D-AUTH-1…7 (`docs/114` §M) **suspended** pending T-1/T-8 |
| **T-1 / T-8 CLOSED · T-2 Operator Staff capabilities — DESIGNED** | **T-1:** `mt_customers` = Operator, vocabulary only. **T-8:** `docs/114` resumed with `dnb_staffauth` and *target operator*. **T-2 (`docs/116`):** C6/C16 answered — `kind owner|staff` (value `operator` renamed: it now means the tenant), `capabilities text[]` checked against one SQL list `mt_op_capabilities()`, owner implies all and `op.staff.manage` is not grantable, at least one active owner per operator, `mt_principal_can()` RLS-bound via `dnb_def_comm`, the six commercial functions gain a capability floor, every `/me` route gains a guard, `resolve_token` returns kind + capabilities live. **Measured hole:** `dnb_app` can `UPDATE` a principal's `kind` and `INSERT` a forged owner within its tenant today (rolled back) — closed by revoking its `mt_principals` writes in the same migration; thirteen more tables still carry `dnb_app` write grants → **B-3**, own task. Location scoping **later** (T-6). No blocker to G-B; sequence T-1 pass → G-B → 027 → G-C. Nothing implemented |
| **T-1 vocabulary pass — DONE, wording only** | **No schema, grant, migration or API change** (`docs/117`). Panel labels say *Operators & sites* / *Operator*; comments and messages that said "operator" for the person at the console now say *DishNet staff* or *the engineer*; manifest prose and `docs/114`/`docs/116` aligned; a canonical vocabulary table and a reading rule for older documents recorded. Left deliberately: the `kind` CHECK value `operator` and the two test fixtures that insert it (**027**), the entitlement key `max_operators` (own decision), the `/customers` API paths and `customer` JSON keys (a later, non-breaking code change), historical documents (not rewritten). 027 pending, B-3 open, the `dnb_app` finding unchanged |
| **G-B — DishNet Staff authentication BUILT (migration 026), not bound by default** | **BUILT in the development schema, W-4 closed in code, production posture unchanged** (`docs/114` §O). `mt_staff` + `mt_staff_sessions` owned by `dnb_def_staff` (created under `SET LOCAL ROLE`, no RLS, **no login role holds any privilege** — enumerated, with a control); pgcrypto, the schema's first extension; bcrypt cost 12 and RFC 6238 TOTP verified **inside `mt_staff_login()`** with the decaying lockout (5 → 15 min, ×2, 24 h ceiling, never permanent), every number in `mt_staff_policy()` only; every failing path spends a bcrypt and returns the empty set → one byte-identical 401. `dnb_staffauth` reaches exactly login/resolve/logout (+ constants), zero table privileges; lifecycle and self-service on `dnb_adminwrite` (W-3 list updated deliberately); opaque 256-bit HttpOnly/Secure/SameSite=Strict cookie stored only as a label-derived HMAC, 8 h, revocable, **issued only over TLS** (403 `insecure_transport` otherwise); second factor required by default; CSRF by Origin + JSON; provider chosen explicitly by `DN_STAFF_IDENTITY=dishnet`, **never by fallback**, refuses to coexist with the dev gate, works under the F6-B gate, fails loudly (500) when misconfigured. Six session routes, seven Admin-only `/staff` routes bound only under the real provider; the seven estate POSTs still 501. Eleven audited acts, `actor_kind='staff'`, no new kind, no secret in detail. Proved in `tests/test_staff_identity.php` (487) and over real HTTP; suite **31 / 2,375 / 0 twice** (was 30 / 1,846). `DenyAllIdentity` remains the default; TLS in front of PHP and `staff:bootstrap` are the deployment's own steps (G-D, not authorised). **Migrations end at 026; no 027; 027 / T-2 / B-3 / G-C / O-1 NOT begun; nothing installed anywhere.** |
| **T-2 — Operator Owner / Staff capability model BUILT (migration 027); B-3 still OPEN** | **BUILT in the development schema only** (`docs/116` §K). `mt_principals.kind` is `owner \| staff` — the rewrite `operator → staff` runs **as `dnb_def_auth`** with a `RAISE NOTICE` census and an assertion, because the owner under FORCE RLS sees **0** rows (proved on a disposable level-026 database seeded with 2 legacy rows: census 2 + 1, rewrote 2, 0 remain, dropped; **NOT production evidence** — census §1c is). `capabilities text[]` checked against `mt_op_capabilities()` (17 names, `OpCapability::ALL` asserted equal); owner implies all and stores `{}`; `op.staff.manage` is never grantable (CHECK); **the last-owner invariant is checked BEFORE the self-guard** — checked after, it could never fire, and a guard that cannot fire cannot be proved. **`dnb_app` holds SELECT only on `mt_principals`** — INSERT/UPDATE/DELETE each proved refused by execution, `dnb_def_comm` (no widening policy, tenant-bound below the function) as the positive control. Writers `mt_principal_create/_set_capabilities/_set_kind/_disable` + `mt_principal_can` → `dnb_app` only; the floor `mt_principal_require` granted to nobody; `mt_auth_resolve_token` returns kind + capabilities **live**; disable revokes every session in the same transaction (proved by rollback). Every `/me` route guarded (`$guard`, a capability is a required argument), five `/me/staff` routes, 403 names the capability, the 404 record rule untouched; `Kernel` maps only `capability required:` 42501 → 403. The six commercial functions carry the floor: 42501, **no audit row**, detail `principal_kind` + `capability`. `mt_admin_principal_create(target operator, …)` on `dnb_def_prov` (INSERT-only policy), never `mt_current_customer()` (asserted on the source), `actor_kind = 'staff'`; `mt_admin_principals()` bound as the **fourteenth** projection, phone/email unreturnable; the Admin creator route declared unbound (501). `mt_op_capabilities()` first PUBLIC-executable — **the S1/S2 guard refused it and was right**; EXECUTE now on exactly three definer roles. Fixtures and simulator create owners through the Admin creator. Suite **32 / 2,769 / 0 twice** (was 31 / 2,375); `test_operator_staff.php` 378. **NOT done, deliberately:** B-3 (thirteen tables asserted unchanged), G-C, G-C2, O-1, G-D, T-6, T-10, T-11, PWA staff screens, `POST /me/staff` idempotency (I-A), F-3. Production: nothing applied, nothing installed, the census still first |
| **G-C — the MikroTik router control-plane boundary BUILT in software; nothing HARDWARE VERIFIED** | **BUILT, development schema only, no migration** (`docs/118`). The four layers of `docs/30` Artifact 3b hold: the control plane's REST adapter and worker are proved, nothing configures HotSpot, publishes AAA or asserts the tunnel. **Destination derived, never accepted:** the device row under the intent's tenant context is the only source of a router's address; twelve payload keys (`host`, `serial`, `password`, …) are a permanent refusal before any connection — proved with a call log that stayed at zero. **Lifecycle gate:** delivery only to `connected`/`provisioned`/`active`/`diverged`; the adapter never moves a device (who moves `connected`/`provisioned` in production is UNRESOLVED). **Identity check:** `system/routerboard` serial must equal the registry's (H8, VERSION/MODEL DEPENDENT; a mismatch makes exactly one call). **The F6-B gate is at the socket** — `RestClient` refuses its real transport without `DN_ALLOW_REAL_BINDINGS`; `DN_DELIVERY` selects `null` / `simulated` / `routeros` and never falls back; `SimulatedRouterOs` refuses to exist in a gated process, writes nothing to the database, and resolves through the same `DeliveryTarget` as the real adapter. Timeouts, malformed answers (never a confirmation) and scrubbed errors proved; every `last_error` of the run carries no credential, address or key. **Admin plane: register and assign BOUND** (`dnb_adminwrite`, W-1 functions, actor = the staff subject, forged `staged_by`/`actor`/`customer_id` fields refused with 400); **the action route NOT bound** — it needs an Admin-plane enqueue function, a migration, and this instruction fixed the state at 027 (`docs/118` D-2, recorded, not worked around); `docs/114` §K G-C is therefore met for two of three verbs and says so. Evidence register H1–H12 + B1 in `docs/118` §C/§F; **the HARDWARE VERIFIED label was awarded to nothing**; `tools/chr_harness.sh` still unrun. Suite **33 / 3,026 / 0 twice** (was 32 / 2,769); `test_router_control_plane.php` 250; G-B 487 and T-2 378 unchanged; install-test 85/85. **Not started:** G-C2, B-3, O-1, G-D, T-6/T-10/T-11, production anything |
| **Physical hardware-test handoff — the HARDWARE VERIFIED gate** | **HANDOFF, documentation only** (`docs/119`). The project is stopped at the physical-hardware gate after G-C (`9a09cc7`). One checklist of every item still *HARDWARE VERIFIED pending*, in eleven categories — router identity · WireGuard transport · REST management · RouterOS provisioning · HotSpot · RADIUS authentication · accounting · disconnect/CoA · reset/recovery · **B1 push-vs-poll** · state ownership / `last_seen_at` — each row with the exact command (quoted from `docs/32`/`docs/35`, every one still an UNVERIFIED proposal), expected observation, evidence, pass and fail conditions, model/version dependence and the decision it gates. Reproduces the `docs/32` §0.1 22-row inventory and the §0.4 export **and restore** test; states the REST surface the adapter would send so the bench exercises it by hand with F6-B closed; records two software facts the evidence must inform (the production `session.disconnect` intent carries only `session_id`; the adapter PATCHes the collection path). B1 is recorded PUSH / POLL / UNRESOLVED with a capture that separates direct reachability from keepalive-refreshed reachability; a handshake alone is not push evidence; no poll architecture follows from a result. CHR is lab evidence only. A result template (`TEST ID · COMMAND · OBSERVATION · EVIDENCE · RESULT · MODEL/VERSION · GATE`) and a *Physical Gate Status* table whose `HARDWARE VERIFIED` column is empty by construction. **Nothing moved: no migration (027 stays last), no code, no production access, no hardware claim; G-C2, B-3, O-1, G-D not started** |
| **Domain-B staging on the existing server — FEASIBILITY AUDITED, NOT deployed** | **READ-ONLY AUDIT** (`docs/120`). This session cannot reach the `dishnetuganda` host, so the server state is the **recorded 19 Sep baseline** (`docs/34`/`36`/`00` §10) plus a **read-only verification script** for the operator; the package was **measured** from the repository (PHP ≥ 8.1 + `pdo_pgsql`/`json`/`openssl`; static panel at the origin root + `api.php` front controller at `/api/v1/admin/*`; one worker; PostgreSQL ≥ 14 on **its own instance** because the 15 roles are cluster-wide; nothing written at runtime; artifact 116 files / 212 KB, content digest `4e7467ad…16c798e`). **Recommended: the Phase-0 pattern** — three plain `docker run` containers (`dnb-staging-postgres` with no published port, `dnb-staging-api` on `127.0.0.1:8099` only, `dnb-staging-worker` with `DN_DELIVERY=simulated`) on a dedicated bridge, outside the swarm, not an EasyPanel service, reached over an SSH tunnel; development identity only, `DenyAllIdentity` unchanged as the default, no `staff:bootstrap`, no real bindings. `portal-staging.dishnetuganda.com` is **designed as a separate stage 2** (DNS record + Traefik route through EasyPanel + IP allow-list **and** basic auth in front of the credential-less identity) and **not authorised**. Exact steps and rollback written, **not executed**; **zero server changes**. **Server state VERIFIED 2026-09-23** by the operator's read-only run (§1.3): baseline confirmed, every threshold met; host PHP lacks `pdo_pgsql` (containers stay recommended); Traefik 3.6.7 is file-configured (a stage-2 route = one YAML under `/etc/easypanel/traefik/config/`); UISP 3.0.159 / uCRM 4.5.33 recorded for `docs/98` Q1. **Stage 1 APPROVED 2026-09-23 and handed over as `docs/120` §15** — block A (read-only before-evidence + go/no-go), block B (the deployment script, stops at the first failure), block C (SSH tunnel + browser) — with **seven amendments to §11** found before any server run, the decisive one being that `install` writes its secrets file *quoted* while Docker's `--env-file` keeps quotes literally, so the containers read an unquoted `secrets.docker.env`. This session cannot run the blocks. **Run record (§15.7):** block A → GO (15:31 UTC); block B attempt 1 passed steps 0–3 (digest match at `7848816`, image 601 MB not ~90 MB, bridge 172.22.0.0/16, PostgreSQL 16.15) and the bootstrap, then **stopped at step 4 on the block's own boolean-format check** — the server state was correct; block R (remove the four inert objects) and block B revision 2 handed over. **STAGE 1 DEPLOYED AND VERIFIED 2026-09-23 15:41 UTC (§15.6):** one clean pass, all nine post-deployment checks hold (three containers Up; PostgreSQL reachable only from the `dnb-staging` bridge, no response from the default bridge or towards `dn-phase0-postgres`/`unms-postgres`; `127.0.0.1:8099` the only new listener; 27 migrations, 16 roles, `mt_staff` 0; all 20 production containers unchanged, StartedAt/RestartCount identical; iptables +7/+4 additions only). **The panel has not been seen: the operator's network drops outbound port 22, so block C's tunnel failed on the Mac** — a phone hotspot is the zero-change path. The operator then asked to *make it publicly accessible*: **stage 2, NOT started** (§15.8) — Traefik + DNS are production boundaries and the dev identity has no credential, so allow-list + basic auth are mandatory; **approved by action** (the operator created the DNS A record); blocks D–G handed over following the live mail-stack precedent (`dishnet-mail/traefik-mail.yml`: file-provider route, `https` entrypoint, `letsencrypt` resolver, backend on the bridge gateway `172.17.0.1`), with an IP allow-list AND basic auth in front and the API never on `0.0.0.0`; then, at the operator's request, the **one-command form** `scripts/dnb-staging-stage2.sh` (allow-list optional, rate limit added, `http` → `https`, idempotent; §15.8.6). **STAGE 2 IN PLACE 2026-09-23 16:32 UTC:** `https://portal-staging.dishnetuganda.com/` behind basic auth with a Let's Encrypt certificate, Traefik not restarted, public 8099 still refused. Simulator finding: its routers' tunnel addresses `10.99.0.x` are outside `10.66.0.0/16`, so its 3 provisioning jobs retry then fail under G-C's address rule |
| **Router lifecycle and provisioning from the Admin plane — BUILT (migration 028); staging redeploy HANDED OVER** | **BUILT, development schema only; nothing installed by this work** (`docs/121`). Steps 1 and 2 of the production roadmap the operator accepted after stage 2. **Migration 028:** `mt_device_provision_request(device, idempotency_key, actor)` — owner `dnb_def_prov`, EXECUTE **`dnb_adminwrite` only**, the operator DERIVED from the device row, payload `{device_id}` only, refuses an unassigned / non-deliverable / decommissioned / address-less router with the reason (nothing queued, nothing audited), replay check BEFORE the insert and the audit (RULE I-1) with the race closed by `ON CONFLICT DO NOTHING`; `dnb_def_prov` gains SELECT+INSERT on `mt_intents` (SELECT because an INSERT-only policy would make the replay check read zero rows — the O-1 lesson); **`mt_device_set_state` is a no-op with no audit row for a state the row already holds** (RULE I-1), grants preserved. **Routes:** `POST /routers/{id}/state` (new capability `routers.lifecycle`, Admin + NOC; records the seven states a person may observe — `diverged` is computed and never recorded by hand, `registered` is the start; migration 012's trigger judges legality → 409 with its own reason) and `POST /routers/{id}/actions` (`routers.act`; `push_config` → 202 new / 200 replay `{intent, replayed}`; `reboot`/`reprovision`/`diagnostics` → 501 with the inventory's reason). `SignalReport` marks exactly `push_config` available. Manifest: four bound router writes, five declared-unbound. **Panel:** a third client `routers.js` (four `POST`s under `/routers`, `api.js` byte-identical and still read-only), *Add a router*, *Assignment*, *Record the next step* (from `NEXT_STATES`, proved by execution against the trigger: 20 offered pairs accepted, 2 non-offered refused), a live *Push configuration* with a per-page idempotency key. **Proved by execution:** the function accepts exactly `DeliveryTarget::DELIVERABLE_STATES` over all nine states; replay writes nothing; queued → retryable under `NullDelivery`, → confirmed under `SimulatedRouterOs` with the device's state unchanged. Suite **34 / 3,262 / 0**, twice (was 33 / 3,026); new suite 214; `install-test.sh` 85/85. **Nothing HARDWARE VERIFIED**; F6-B still NOT AUTHORIZED; `docs/119` unchanged. **Staging redeploy:** one command, `scripts/dnb-staging-redeploy.sh`, pinned to content digest `1bc95524…7af74b`; **result PENDING**. Not done: desired-state authoring, the other three actions, device-state automation, the simulator's `10.99.0.x` addresses, G-C2, B-3, O-1, G-D, NAS/RADIUS |
| **Unanswered factual input** | whether a site may have several MikroTik HotSpot routers (docs/68 §2.6b). It did **not** block Decision 2a and is **not** settled by it |
