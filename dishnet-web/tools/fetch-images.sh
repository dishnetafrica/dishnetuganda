#!/usr/bin/env bash
# OBSOLETE since 25 Aug 2026: the site references no remote images at all.
# Logos are text, the share image is generated locally (assets/img/og-dishnet.png),
# and photos were replaced with inline line art. verify-site.sh fails the build
# if any dishnetafrica.com or portal.dishnetss.com reference ever returns.
echo "Nothing to do - the site is self-contained."
